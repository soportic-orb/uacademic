import { CalendarView } from '../features/calendar/calendar-view'

/** The teacher's own timetable, in the four views the product asks for. */
export function CalendarPage() {
  return <CalendarView />
}
